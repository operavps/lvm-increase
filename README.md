# lvm-increase
A ready to use script for linux distros to mount &amp; increase the LVM disk size on root

This script automates the process of increasing disk size on various Linux distributions, including Ubuntu, Debian, Fedora, CentOS, AlmaLinux, Rocky Linux, and Arch Linux. 

It includes functions for detecting the operating system, checking for LVM, resizing partitions, and extending the filesystem.
